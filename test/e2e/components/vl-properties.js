const { VlElement } = require('vl-ui-core').Test;

class VlProperties extends VlElement {  
    constructor(driver, selector) {
        super(driver, selector);
    }

}

module.exports = VlProperties;
